{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "local-memory": {
      "type": "local",
      "command": ["${env:PROJECT_PATH}/target/release/local-memory"],
      "enabled": true,
      "environment": {
        "LOCAL_MEMORY_CONFIG": "${env:PROJECT_PATH}/.local-memory/config.json"
      }
    }
  }
}
