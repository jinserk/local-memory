{
  "mcpServers": {
    "local-memory": {
      "command": "cargo",
      "args": [
        "run",
        "--quiet",
        "--release",
        "--bin",
        "local-memory"
      ],
      "env": {
        "LOCAL_MEMORY_CONFIG": "${PROJECT_PATH}/.local-memory/config.json"
      }
    }
  }
}
