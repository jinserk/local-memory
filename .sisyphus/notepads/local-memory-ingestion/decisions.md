## Decisions
- Split `AGENTS.md` into 6 logical sections for ingestion.
- Used a Python helper script to bypass `skill_mcp` connectivity issues.
- Verified ingestion using `memory_search` with a specific query.
